module caojiji_FSM(input        Clk,                // 50 MHz clock
                             Reset,              // Active-high reset signal
                             frame_clk,          // The clock indicating a new frame (~60Hz)
					 input logic [5:0]sw,
					 output	[5:0] saber_state);

endmodule